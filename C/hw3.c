/*#include <stdio.h>
#include <math.h>

double dist_2d(double x1, double y1, double x2, double y2) {
    return sqrt(pow(x2 - x1, 2) + pow(y2 - y1, 2));
}

int main() {
    double x1, y1, x2, y2;

    printf("첫번째 점의 좌표를 입력하시오:(x,y)\n");
    scanf("%lf %lf", &x1, &y1);
    printf("두번째 점의 표를 입력하시오:(x,y)\n");
    scanf("%lf %lf", &x2, &y2);

    double result = dist_2d(x1, y1, x2, y2);

    printf("두 점 사이의 거리: %lf\n",  result);

    return 0;
}*/
