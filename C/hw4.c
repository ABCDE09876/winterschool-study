/*#include <stdio.h>
#include <math.h>

void quad_eqn(double a, double b, double c) {
  
    double discriminant = b * b - 4 * a * c;

    if (discriminant >= 0) {
        double root1 = (-b + sqrt(discriminant)) / (2 * a);
        double root2 = (-b - sqrt(discriminant)) / (2 * a);

        printf("근 1: %.2f\n", root1);
        printf("근 2: %.2f\n", root2);
    } else {
        printf("판별식이 음수이므로 근이 없습니다.\n");
    }
}

int main() {
    double a, b, c;

    printf("2차방정식의 계수를 입력하시오:\n");
    printf("a: ");
    scanf("%lf", &a);
    printf("b: ");
    scanf("%lf", &b);
    printf("c: ");
    scanf("%lf", &c);

    quad_eqn(a, b, c);

    return 0;
}*/

