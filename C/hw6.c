/*#include <stdio.h>

void show_digit(int x) {
    printf("%d ", x % 10);

    if (x / 10 != 0) {
        show_digit(x / 10);
    }
}

int main() {
    int number;
    printf("정수를 입력하시오:");
    scanf(" %d", &number);

    show_digit(number);

    return 0;
}
*/